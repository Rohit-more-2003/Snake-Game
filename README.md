# Snake Game — Flask Edition 🐍

A classic Snake game implemented with HTML5 Canvas + JavaScript as the front-end,
and Flask (Python) as the back-end.  
This project is mainly for learning — combining small JS game logic with a web server.

---

## Features

- Classic Snake movement
- Score tracking
- Restart button
- Served via Flask backend
- Static JS/CSS routed via `url_for` properly
- Lightweight — no database required

---

## Project Structure

- app.py
- game.js
- index.html
- README.md

---

## Run Locally

### 1) Create venv (optional but recommended)

```bash
python3 -m venv venv
source venv/bin/activate      # mac/linux
# on Windows: venv\Scripts\activate
